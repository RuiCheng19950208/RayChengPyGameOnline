# 坦克游戏配置文件示例
# 复制此文件为 .env 来使用这些配置
# Tank Game Configuration Example
# Copy this file as .env to use these settings

# 游戏基础配置 - Game Basic Settings
SCREEN_WIDTH=800
SCREEN_HEIGHT=600
FPS=60
TANK_SPEED=300
BULLET_SPEED=300
BULLET_DAMAGE=25
BULLET_LIFETIME=5.0

# 服务器配置 - Server Settings
SERVER_HOST=0.0.0.0
SERVER_PORT=8765
MAX_PLAYERS_PER_ROOM=8

# 分辨率自适应配置 - Resolution Adaptation Settings
# 启用分辨率自适应，自动调整窗口大小以适应不同屏幕（默认关闭，保持固定窗口大小）
ENABLE_RESOLUTION_ADAPTATION=false
# 最小缩放因子，防止窗口过小
MIN_SCALE_FACTOR=0.8
# 最大缩放因子，防止窗口过大
MAX_SCALE_FACTOR=2.0

# 字体配置 - Font Settings
DEFAULT_FONT_PATH=../assets/STHeiti Light.ttc

# 网络配置示例 - Network Configuration Examples
# 本机运行服务器时使用：
# SERVER_HOST=0.0.0.0

# 连接到远程服务器时，客户端可以使用：
# SERVER_HOST=192.168.1.100  # 替换为实际服务器IP 